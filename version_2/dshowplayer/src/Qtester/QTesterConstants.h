#pragma once

class QTesterConstants
{
public:
	const unsigned short MAX_PATH_LENGTH = 256;
	const unsigned short MAX_LABEL_LENGTH = 256;
	const unsigned short MAX_OPTION_POSSIBLE = 256;
	const unsigned short MAX_PLAYLIST_LENGTH = 256;
};